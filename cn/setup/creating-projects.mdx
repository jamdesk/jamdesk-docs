---
title: 创建项目
description: 从模板创建 Jamdesk 项目，或连接现有 GitHub 仓库，让网站在几分钟内上线。
---

开始编写文档前，您需要先创建一个项目。您可以从模板开始（Jamdesk 会在 GitHub 或 Bitbucket Cloud 上为您创建仓库），也可以连接已有 `docs.json` 的现有仓库。无论选择哪种方式，您的网站都能在几分钟内上线。

## 创建新项目

<Steps>
  <Step title="登录 Jamdesk">
    前往 [dashboard.jamdesk.com](https://dashboard.jamdesk.com)，使用您的 GitHub 账户登录。

    首次使用时，系统会提示您授权 Jamdesk GitHub App。
  </Step>

  <Step title="点击 New Project">
    在仪表板右上角点击 **New Project**。
  </Step>

  <Step title="选择起始方式">
    <Tabs>
      <Tab title="From Template">
        从预构建模板开始。Jamdesk 会为您创建一个新的 GitHub 仓库，其中已配置示例内容、导航和组件。

        默认的 [Starter Template](https://github.com/jamdesk/starter-docs) 包含示例页面、OpenAPI 规范以及每个组件的示例。将内容替换为您自己的内容即可完成。
      </Tab>
      <Tab title="From Repository">
        连接现有 GitHub 仓库：

        1. 从下拉菜单中选择仓库
        2. 选择要部署的分支（通常为 `main`）
        3. 如果 `docs.json` 不在根目录中，请指定其路径

        如果仓库位于 Bitbucket Cloud，请改为输入 `workspace/repository-slug`。请参阅[连接 Bitbucket](/cn/setup/connecting-bitbucket)。
      </Tab>
    </Tabs>
  </Step>

  <Step title="配置项目设置">
    设置项目的基本信息：

    | 字段 | 说明 |
    |-------|-------------|
    | **Name** | 在仪表板中显示的名称 |
    | **Slug** | 适合用于 URL 的标识符（例如 `my-docs`） |
    | **Domain** | 您的 `*.jamdesk.app` 子域名 |
  </Step>
</Steps>

## 最小仓库结构

Jamdesk 项目只需要一个 `docs.json` 和至少一个页面：

```bash
my-docs/
├── docs.json
└── introduction.mdx
```

`docs.json` 示例：

```json docs.json
{
  "$schema": "https://jamdesk.com/docs.json",
  "name": "My Docs",
  "theme": "jam",
  "navigation": {
    "groups": [
      {
        "group": "Getting Started",
        "pages": ["introduction"]
      }
    ]
  }
}
```

## 项目设置

创建项目后，在 **Settings** 中配置项目：

### General

- **Name** - 更新显示名称
- **Slug** - 更改项目标识符
- **Delete Project** - 永久删除项目

### Repository

- **Branch** - 触发部署的分支
- **Docs Path** - 单体仓库中 `docs.json` 的位置

### Domain

- **Subdomain** - 您的 `*.jamdesk.app` URL
- **Custom Domain** - 添加您自己的域名
- **Host at a subpath** - 将文档部署在 `/docs` 或 `/help` 等路径下，而不是网站根路径

### Build

- **Show Jamdesk Branding** - 在页脚中显示“Powered by Jamdesk”链接（默认启用）
- **Rebuild** - 手动触发新的构建

<Note>
构建设置的更改需要新建一次构建才能生效。
</Note>

## 项目限制

| 计划 | 项目数 | 每月构建数 |
|------|----------|--------------|
| Free | 1 | 100 |
| Pro | 10 | Unlimited |
| Team | Unlimited | Unlimited |

## 接下来做什么？

<Columns cols={2}>
  <Card title="连接 GitHub" icon="github" href="/cn/setup/connecting-github">
    关联仓库以自动构建
  </Card>
  <Card title="目录结构" icon="folder-tree" href="/cn/setup/directory-structure">
    构建可扩展的文档结构
  </Card>
  <Card title="单体仓库设置" icon="folders" href="/cn/setup/monorepo-support">
    为单体仓库配置文档路径
  </Card>
  <Card title="端到端教程" icon="route" href="/cn/end-to-end-tutorial">
    按照完整的 Jamdesk 工作流程操作
  </Card>
</Columns>