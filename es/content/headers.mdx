---
title: Encabezados
description: Estructura las páginas con encabezados H2 y H3 que se agregan automáticamente a la tabla de contenidos del lado derecho y generan enlaces de anclaje compartibles.
---

Los encabezados Markdown (`##`, `###`) dividen tu página en secciones que los lectores pueden escanear rápidamente. Jamdesk toma los encabezados H2 y H3 para la tabla de contenidos del lado derecho y genera enlaces de anclaje que puedes compartir directamente.

## Uso básico

Usa encabezados Markdown estándar para estructurar tu contenido:

```markdown
## Section Title

Content goes here...

### Subsection

More detailed content...
```

Los encabezados H2 y H3 aparecen en la **Tabla de contenidos** del lado derecho, lo que ayuda a los lectores a navegar por páginas largas.

## Enlaces de anclaje

Cada encabezado genera automáticamente un ID de anclaje a partir de su texto:

| Encabezado                | Anclaje generado       |
| ------------------------- | ---------------------- |
| `## Getting Started`      | `#getting-started`     |
| `## API Reference`        | `#api-reference`       |
| `### User Authentication` | `#user-authentication` |

### Compartir enlaces de encabezado

Pasa el cursor sobre cualquier encabezado H2 o H3 para revelar un **icono de enlace**. Esto facilita compartir enlaces directos a secciones específicas:

- **Haz clic en el icono de enlace** para copiar la URL al portapapeles
- **Haz clic en el texto del encabezado** para copiar la URL y navegar a esa sección

El icono de enlace aparece en un pequeño cuadro con borde a la izquierda del encabezado. Cuando copias un enlace, el icono cambia brevemente de color para confirmar la acción.

<Note>
  En dispositivos móviles, el icono de enlace está oculto ya que no hay estado de hover.
  Los encabezados siguen funcionando normalmente para la navegación.
</Note>

### Enlazar a encabezados

Referencia secciones específicas desde otras páginas usando el anclaje:

```markdown
See the [headings section](/content/mdx-basics#headings) for details.
```

## Buenas prácticas

<AccordionGroup>
  <Accordion title="Usa una jerarquía clara" icon="sitemap" defaultOpen>
    Comienza con H2 para las secciones principales y H3 para las subsecciones:

    ```markdown
    ## Installation

    ### Prerequisites

    ### Quick Install

    ## Configuration

    ### Basic Options

    ### Advanced Options
    ```

    Evita saltarte niveles (por ejemplo, pasar de H2 a H4).

  </Accordion>

  <Accordion title="Mantén los encabezados concisos" icon="text-width">
    Los encabezados cortos son más fáciles de escanear:

    ```markdown
    {/* Good */}
    ## Authentication

    {/* Avoid */}
    ## How to Set Up Authentication in Your Application
    ```

  </Accordion>

  <Accordion title="Usa mayúscula de oración" icon="font">
    Escribe en mayúscula solo la primera palabra y los nombres propios:

    ```markdown
    {/* Good */}
    ## Getting started with the API

    {/* Avoid */}
    ## Getting Started With The API
    ```

  </Accordion>
</AccordionGroup>

## ¿Qué sigue?

<Columns cols={2}>
  <Card title="Optimización SEO" icon="magnifying-glass-chart" href="/es/content/seo">
    Mejora el descubrimiento y los metadatos
  </Card>
  <Card title="Frontmatter" icon="file-lines" href="/es/content/frontmatter">
    Define títulos, descripciones y campos SEO
  </Card>
</Columns>
