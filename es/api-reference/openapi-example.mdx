---
title: Ejemplo de OpenAPI
description: Visualiza un endpoint generado en vivo desde OpenAPI y aprende cómo Jamdesk renderiza solicitudes, respuestas y autenticación desde tu spec.
openapi: /openapi/example-api.yaml POST /tickets
---

Esta página muestra un endpoint en vivo generado a partir de una [especificación OpenAPI](https://spec.openapis.org/oas/latest.html). El esquema de solicitud, los modelos de respuesta y los ejemplos de código del panel derecho se generan automáticamente desde el spec, sin necesidad de redacción manual.

<Info>
Este ejemplo usa la API de soporte de Acme. Actualiza `api.openapi` en tu `docs.json` para apuntar a tu propio archivo spec y generar endpoints reales.
</Info>

<Info>
**¿Documentación en varios idiomas?** Proporciona un archivo `<spec>.<lang>.<ext>` junto a tu spec fuente (p. ej., `example-api.fr.yaml`) y Jamdesk renderiza la versión traducida cuando los usuarios visitan la página bajo `/fr/...`. Consulta [Traducir specs de OpenAPI](/es/setup/languages#traducción-de-especificaciones-openapi).
</Info>

<Tip>
Esta página tiene el [API Playground](/es/api-reference/playground) habilitado. Haz clic en **Try it** en el endpoint anterior para probar la API en vivo.
</Tip>

## Qué se genera

Con una sola línea `openapi` en el frontmatter, Jamdesk genera automáticamente:

- Un badge de endpoint que muestra el método y la ruta con codificación de color
- Documentación de parámetros de ruta, consulta, cabecera y cuerpo
- Esquemas de solicitud y respuesta, incluidos objetos anidados y arrays
- Ejemplos de código en cURL, Python, JavaScript, Go, Ruby, C#, Java, Rust y PHP (configurable mediante `api.examples.languages`)
- Detalles de autenticación extraídos de los esquemas de seguridad del spec

Todas las referencias `$ref` en tu spec se resuelven automáticamente, por lo que puedes organizar los esquemas con `components/schemas` de la forma habitual.

## Configurar OpenAPI

Coloca tu spec OpenAPI 3.x (YAML o JSON) en el directorio `openapi/`, regístralo en `docs.json` bajo `api.openapi` y agrega `openapi: /openapi/your-spec.yaml METHOD /path` al frontmatter de cualquier página. Consulta la [guía de configuración de OpenAPI](/es/setup/directory-structure#openapi) para más detalles.

<Tip>
  ¿Escribes tu spec en YAML? Ejecútalo a través del [Validador de YAML](https://jamdesk.com/utilities/yaml-validator) gratuito para detectar errores de indentación y sintaxis antes de que el build lo procese.
</Tip>

## Páginas relacionadas

<Columns cols={2}>
  <Card title="API Playground" icon="flask-vial" href="/es/api-reference/playground">
    Habilita pruebas interactivas de API en tus páginas de endpoints
  </Card>
  <Card title="Ejemplos de solicitud/respuesta" icon="code" href="/es/api-reference/request-response-examples">
    Ejemplo de endpoint redactado manualmente con componentes MDX
  </Card>
</Columns>

<Columns cols={2}>
  <Card title="Configuración de OpenAPI" icon="file-code" href="/es/setup/directory-structure#openapi">
    Dónde almacenar y referenciar archivos OpenAPI
  </Card>
  <Card title="Referencia de docs.json" icon="file-lines" href="/es/config/docs-json-reference">
    Referencia de configuración completa, incluido api.openapi
  </Card>
</Columns>
