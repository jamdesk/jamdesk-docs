---
title: llms.txt
sidebarTitle: llms.txt
description: Todos os sites Jamdesk geram automaticamente llms.txt e llms-full.txt. As ferramentas de IA usam esses arquivos para entender sua documentação.
---

Se você quer que ferramentas de IA como ChatGPT, Claude ou Copilot forneçam respostas precisas sobre seu produto, elas precisam de acesso estruturado à sua documentação, não apenas do conteúdo incluído nos dados de treinamento.

Todo site Jamdesk gera `llms.txt` e `llms-full.txt` seguindo a especificação [llmstxt.org](https://llmstxt.org). As ferramentas de IA leem esses arquivos em vez de rastrear páginas individuais.

<Note>
  Atualmente, o Google audita `llms.txt` como parte das [verificações de navegação agêntica do Lighthouse](https://developer.chrome.com/docs/lighthouse/agentic-browsing/llms-txt). Sites que publicam um `llms.txt` válido são mais descobertos por agentes de IA e, cada vez mais, pelo próprio Google ao classificar conteúdo para respostas com IA. O Jamdesk oferece isso gratuitamente.
</Note>

<Tip>
  **Verifique sua pontuação.** Veja como qualquer site de documentação está preparado para agentes com a ferramenta gratuita [AI Score](https://jamdesk.com/ai-score). A publicação de um `llms.txt` válido é uma das primeiras verificações realizadas. Também há uma [extensão AI Score para Chrome](https://chromewebstore.google.com/detail/jamdesk-ai-score/mhihkkgpcbmapmojnakhjfhjmiagkhlj) para avaliar sites enquanto você navega.
</Tip>

## O que o Jamdesk gera

| Arquivo | Conteúdo |
|------|---------|
| `llms.txt` | Títulos, descrições e URLs das páginas (um índice estruturado) |
| `llms-full.txt` | Conteúdo completo da documentação em um único arquivo |

O Jamdesk gera ambos durante o build e os disponibiliza pelo mesmo CDN que sua documentação.

## Verifique seus arquivos

Confirme que seus arquivos existem após o primeiro build:

<Tabs>
  <Tab title="Domínio raiz">
    Se sua documentação estiver na raiz do domínio (por exemplo, `docs.acme.com` ou `acme.jamdesk.app`):

    ```bash
    curl https://docs.acme.com/llms.txt
    curl https://docs.acme.com/llms-full.txt
    ```
  </Tab>
  <Tab title="Subcaminho /docs">
    Se sua documentação estiver em `/docs` no site principal (como neste site, em `jamdesk.com/docs`):

    ```bash
    curl https://jamdesk.com/docs/llms.txt
    curl https://jamdesk.com/docs/llms-full.txt
    ```
  </Tab>
</Tabs>

## Formato do arquivo

O arquivo `llms.txt` lista cada página com seu título, descrição e URL, agrupados em seções que refletem sua navegação:

```text
# Your Project Documentation

> Documentation for Your Project

## Get Started

- [Getting Started](https://your-project.jamdesk.app/getting-started): Set up your first project in 5 minutes
- [Configuration](https://your-project.jamdesk.app/configuration): Configure your project settings

## API Reference

- [Authentication](https://your-project.jamdesk.app/authentication): How to authenticate API requests
```

O arquivo `llms-full.txt` contém o conteúdo completo em Markdown de cada página, concatenado com limites de página marcados por títulos.

## Estrutura

`llms.txt` segue a proposta [llmstxt.org](https://llmstxt.org/): um título H1,
um resumo em bloco de citação e suas abas e grupos de navegação como seções `##` de
links em Markdown.

## Sites multilíngues

Sites com mais de um idioma recebem um conjunto separado de arquivos para cada
idioma, para que uma ferramenta de IA possa buscar apenas o idioma necessário. Para um site em
inglês (o padrão) e francês:

| URL | Conteúdo |
|-----|---------|
| `/llms.txt` | Índice em inglês, com links para o índice de cada outro idioma no final |
| `/en/llms.txt` | Mesmo arquivo que `/llms.txt` |
| `/fr/llms.txt` | Índice em francês, com links de volta para os outros idiomas |
| `/llms-full.txt` | Conteúdo completo de todos os idiomas, um após o outro |
| `/en/llms-full.txt` | Somente o conteúdo das páginas em inglês |
| `/fr/llms-full.txt` | Somente o conteúdo das páginas em francês |

O `llms-full.txt` raiz mantém todos os idiomas, portanto as ferramentas que já o leem
não percebem nenhuma mudança. Aponte um agente para o arquivo do próprio idioma quando ele precisar apenas desse idioma:

```bash
curl https://jamdesk.com/docs/fr/llms-full.txt
```

Um idioma recebe seus próprios arquivos assim que tiver pelo menos uma página. Sites com
um único idioma mantêm apenas os arquivos raiz.

## Diretiva para agentes em cada página

Cada página também contém uma nota invisível e silenciosa para leitores de tela, direcionada aos agentes de IA e apontando para `llms.txt`, e cada exportação em Markdown começa com um link em bloco de citação para o índice. Os agentes que acessarem seu HTML encontrarão o caminho do Markdown sem precisar rastrear o site.

## Como isso é criado

- Gerado durante o build junto com seu HTML
- Disponibilizado pelo CDN com a mesma política de cache da sua documentação
- Atualizado automaticamente em cada build
- Nenhuma configuração necessária; todas as páginas são incluídas por padrão

## Desativar o llms.txt

`llms.txt` e `llms-full.txt` são publicados por padrão. Para interromper a geração deles (por exemplo, se você não quiser que ferramentas de IA ingiram seu conteúdo), defina `seo.ai.llmsTxt` como `false` no seu `docs.json`:

```json docs.json
{
  "seo": {
    "ai": {
      "llmsTxt": false
    }
  }
}
```

No próximo build, o Jamdesk ignora os dois arquivos, incluindo cada cópia por idioma, e as URLs ativas `/llms.txt` e `/llms-full.txt` retornam `404`.

<Note>
  **A ingestão por IA e a indexação de busca são controles separados.** `seo.ai.llmsTxt` controla apenas os arquivos `llms.txt`/`llms-full.txt` lidos pelas ferramentas de IA. Isso não altera a forma como os mecanismos de busca indexam seu site; esse comportamento é controlado pelos [`metadados robots e noindex por página`](/pt/content/seo#controlando-a-indexação). Uma página pode ser indexada pelo Google enquanto é excluída da ingestão por IA, ou vice-versa.
</Note>

## Rastreadores de IA

O `robots.txt` padrão do Jamdesk permite todos os principais rastreadores de IA (Claude, ChatGPT, Copilot, Gemini). Sua documentação fica acessível às ferramentas de IA imediatamente.

<Accordion title="Como llms.txt difere de sitemap.xml">
  `sitemap.xml` lista URLs para que os mecanismos de busca rastreiem e indexem. Ele não inclui conteúdo, apenas links e metadados, como datas da última modificação.

  `llms.txt` foi desenvolvido para modelos de IA. Ele inclui títulos e descrições das páginas e, em `llms-full.txt`, o conteúdo propriamente dito. As ferramentas de IA podem ler o arquivo completo para entender sua documentação sem fazer várias solicitações HTTP.
</Accordion>

## O que vem a seguir?

<Columns cols={2}>
  <Card title="AI Score" icon="gauge-high" href="/pt/ai/ai-score">
    Veja como sua documentação está preparada para agentes, com uma pontuação em cada build
  </Card>
  <Card title="AI Chat" icon="comment" href="/pt/ai/chat">
    Assistente de chat integrado para perguntas dos visitantes
  </Card>
  <Card title="MCP Server" icon="robot" href="/pt/ai/mcp-server">
    Permita que as ferramentas de IA pesquisem e consultem sua documentação interativamente
  </Card>
  <Card title="Markdown Source" icon="file-code" href="/pt/ai/markdown-source">
    Busque o Markdown bruto de páginas individuais
  </Card>
</Columns>