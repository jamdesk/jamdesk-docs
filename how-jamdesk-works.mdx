---
title: How Jamdesk Works
description: How Jamdesk builds and deploys your documentation. Understand the pipeline from git push to live site.
---

Unlike static site generators that require build pipelines and CI/CD configuration, or hosted platforms that store content in proprietary formats, Jamdesk works directly from your MDX files in GitHub. Push your changes, and Jamdesk handles building, optimizing, and deploying to a global CDN — typically in under 60 seconds.

## Architecture Overview

<Frame caption="Jamdesk build and deploy pipeline">
  <Mermaid minWidth="700px">
{`%%{init: {'theme': 'neutral', 'themeVariables': {'background': 'transparent'}, 'flowchart': {'nodeSpacing': 25, 'rankSpacing': 40, 'curve': 'basis', 'useMaxWidth': false}}}%%
flowchart LR
    subgraph Source["Your Repository"]
        A[("MDX Files")]
        B[("docs.json")]
    end

    subgraph Jamdesk["Jamdesk Platform"]
        C["Build Service"]
        D["Validation"]
        E["Compilation"]
    end

    subgraph Delivery["Global Delivery"]
        F[("Cloudflare R2")]
        G["Edge Cache"]
    end

    Source -->|"git push"| C
    C --> D --> E
    E -->|"Deploy"| F
    F --> G
    G -->|"< 100ms"| H["Your Users"]`}
  </Mermaid>
</Frame>

## The Build Pipeline

<Steps>
  <Step title="Source Detection">
    Jamdesk monitors your connected GitHub branch. When you push changes, a webhook triggers the build pipeline automatically. You can also trigger builds manually from the dashboard or via API.
  </Step>
  <Step title="Content Retrieval">
    The build service clones your repository and locates your documentation—either at the root or in a subdirectory for monorepo setups.
  </Step>
  <Step title="Validation">
    Before building, Jamdesk validates your configuration:
    - `docs.json` syntax and schema
    - All pages referenced in navigation exist
    - MDX syntax is valid
    - OpenAPI specs parse correctly (if configured)
  </Step>
  <Step title="Compilation">
    MDX files are compiled to optimized HTML with:
    - React components rendered to static HTML
    - Syntax highlighting applied via Shiki
    - Search index generated
    - Meta tags and Open Graph images created
  </Step>
  <Step title="Deployment">
    The built site uploads to Cloudflare R2, then propagates to edge locations globally.
  </Step>
</Steps>

## What Triggers a Build

Builds are triggered by git push, the dashboard Rebuild button, or the [Builds API](/reference/builds-api). Consecutive pushes are debounced into a single build.

For operational details—status monitoring, rollbacks, environment variables, and branch deployments—see [Build & Deploy Lifecycle](/deploy/lifecycle).

## Security & Infrastructure

<Columns cols={2}>
  <Card title="Source Protection" icon="shield-check">
    GitHub App uses minimal permissions—read-only access to your selected repositories.
  </Card>
  <Card title="Build Isolation" icon="box">
    Each build runs in an isolated container. Your code never touches other customers' builds.
  </Card>
  <Card title="Encrypted Delivery" icon="lock">
    All traffic served over HTTPS with automatic SSL certificates.
  </Card>
  <Card title="Edge Network" icon="globe">
    Built sites deploy to Cloudflare's network with 300+ edge locations.
  </Card>
</Columns>

## Performance

| Metric | Typical Value |
|--------|---------------|
| Build time | 30-60 seconds |
| Deploy propagation | < 30 seconds |
| First byte (edge) | < 50ms |
| Full page load | < 500ms |

## Next Steps

<Columns cols={2}>
  <Card title="Create a Project" icon="plus" href="/setup/creating-projects">
    Set up a new documentation site in the dashboard
  </Card>
  <Card title="Connect GitHub" icon="github" href="/setup/connecting-github">
    Link your repository to enable automatic builds
  </Card>
</Columns>
