---
title: OpenAPI Example
description: See a live OpenAPI-generated endpoint page and learn how Jamdesk renders requests, responses, and authentication directly from your spec.
openapi: /openapi/acme-support.yaml POST /tickets
---

This page demonstrates a live endpoint generated from an [OpenAPI specification](https://spec.openapis.org/oas/latest.html). The request schema, response models, and code examples in the right panel are all auto-generated from the spec — no manual authoring required.

<Info>
This example uses the Acme Support API. Update `api.openapi` in `docs.json` to point at your own spec file to generate real endpoints.
</Info>

## How OpenAPI Works in Jamdesk

Jamdesk turns your OpenAPI spec into fully interactive endpoint pages. Here's how to set it up:

### 1. Add Your Spec File

Place your OpenAPI 3.x spec (YAML or JSON) in the `openapi/` directory:

```
openapi/
  acme-support.yaml    # Your OpenAPI spec
```

### 2. Register in docs.json

Point your config at the spec file:

```json docs.json
{
  "api": {
    "openapi": ["/openapi/acme-support.yaml"]
  }
}
```

### 3. Reference in Frontmatter

Each MDX page that renders an endpoint uses the `openapi` frontmatter field:

```mdx
---
title: Create Ticket
openapi: /openapi/acme-support.yaml POST /tickets
---
```

The format is: `<spec-path> <METHOD> <endpoint-path>`.

### What Gets Generated

From a single `openapi` frontmatter line, Jamdesk auto-generates:

- **Endpoint badge** — method and path with color coding
- **Parameter documentation** — path, query, header, and body parameters
- **Request/response schemas** — including nested objects and arrays
- **Code examples** — cURL, Python, JavaScript, and more
- **Authentication details** — pulled from the spec's security schemes

All `$ref` references in your spec are automatically resolved, so you can organize schemas with `components/schemas` as usual.

## What This Page Shows

Scroll to the right panel to see the auto-generated endpoint reference for `POST /tickets`. This includes the request body schema, example responses (201 and 400), and ready-to-use code snippets.

## Related Pages

<Columns cols={2}>
  <Card title="Request/Response Examples" icon="code" href="/api-reference/request-response-examples">
    Hand-authored endpoint example using MDX components
  </Card>
  <Card title="OpenAPI Setup" icon="file-code" href="/setup/directory-structure#openapi">
    Where to store and reference OpenAPI files
  </Card>
</Columns>

<Columns cols={2}>
  <Card title="docs.json Reference" icon="file-lines" href="/config/docs-json-reference">
    Full configuration reference including api.openapi
  </Card>
  <Card title="Examples Component" icon="code" href="/components/examples">
    Use RequestExample and ResponseExample for manual endpoints
  </Card>
</Columns>
