---
title: Builds API / Triggers
description: Use the Builds API to trigger documentation rebuilds without pushing new commits. Use it as a quick lookup when configuring builds or integrations.
---

Use the Builds API to trigger documentation rebuilds without pushing new commits. This is useful for CI pipelines, scheduled rebuilds, or content generated outside your docs directory.

## Authentication

All requests use a project API key:

```
Authorization: Bearer YOUR_API_KEY
```

Find your API key in **Settings** → **API** for the project.

## Trigger a Rebuild

**POST** `https://api.jamdesk.com/projects/{project_id}/rebuild`

### Example Request

```bash
curl -X POST "https://api.jamdesk.com/projects/YOUR_PROJECT_ID/rebuild" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

### Response

Successful requests return `200 OK`. The response includes build metadata you can use to track progress in the dashboard. If you need real-time updates, monitor the build in **Deployments** or check the commit status in GitHub.

<Note>
If you receive a `429` response, retry with exponential backoff. Contact support if you need higher limits.
</Note>

## Common Errors

| Status | Meaning | Fix |
|--------|---------|-----|
| **401** | Invalid API key | Regenerate the key in Settings → API |
| **403** | Unauthorized project | Confirm project ID and access |
| **404** | Project not found | Verify the project ID in the dashboard |
| **429** | Rate limited | Back off and retry |

## Related Articles

<Columns cols={2}>
  <Card title="Triggering Builds" icon="hammer" href="/help/builds/triggering">
    Manual and automatic build triggers
  </Card>
  <Card title="Monitoring Builds" icon="chart-line" href="/help/builds/monitoring">
    Track build status and queue
  </Card>
</Columns>
